{
    // See https://go.microsoft.com/fwlink/?LinkId=733558 
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "format all",
            "type": "shell",
            "windows": {
                "command": "python3 ${workspaceFolder}\\format-all.py"
            },
            "linux": {
                "command": "python3 ${workspaceFolder}/format-all.py"
            },
            "osx": {
                "command": "python3 ${workspaceFolder}/format-all.py"
            }
        }
    ]
}